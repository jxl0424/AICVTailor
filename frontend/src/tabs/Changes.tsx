import { Placeholder } from "../components/Placeholder";

export function Changes() {
  return (
    <Placeholder phase="Phase 5">
      Side-by-side original versus tailored, word-level diff grouped by section and entry.
      Each change shows the JD terms it targets and the source bullet it came from, so
      every rewrite is traceable.
    </Placeholder>
  );
}
