import { Placeholder } from "../components/Placeholder";

export function Library() {
  return (
    <Placeholder phase="Phase 6">
      Every tailored resume, filterable by company, role and date, each row linking to its
      JD, diff, coverage delta and downloads.
    </Placeholder>
  );
}
