import { Placeholder } from "../components/Placeholder";

export function Tailor() {
  return (
    <Placeholder phase="Phase 3">
      Paste a job description, pick a master resume, and see the ranked term table and
      keyword coverage. Rewriting arrives in Phase 5; this tab first has to prove the
      extraction is good, because everything downstream depends on it.
    </Placeholder>
  );
}
